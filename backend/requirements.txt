# Core framework
Django==6.0.6
djangorestframework==3.17.1
django-cors-headers==4.9.0
gunicorn==23.0.0

# Async pipeline
celery==5.5.2
redis==5.2.1
billiard==4.2.1
kombu==5.5.3
vine==5.1.0

# Repo fetching / analysis
requests==2.32.5
GitPython==3.1.43
lxml==6.0.2
PyYAML==6.0.3

# AI review layer (Gemini)
google-genai>=0.3.0
google-auth==2.48.0

# Config / env
python-dotenv==1.2.2

# Auth / crypto (OTP hashing, password validators)
bcrypt==4.0.1
cryptography==46.0.5

# PDF export (analyzer/services/export_service.py imports this directly —
# confirmed via import scan, kept unlike the other document libs below)
reportlab==4.2.5

# ── The following were listed in the original requirements.txt but are
#    not imported anywhere in this codebase. Re-add only if a feature
#    actually starts using them — each one is extra CVE surface and
#    install time for nothing otherwise:
#
#    fastapi, fastapi-cli, starlette, uvicorn, httptools, watchfiles,
#    websockets            → this is a Django app, not FastAPI
#    SQLAlchemy, pymongo    → this app only talks to the Django ORM/SQLite
#    python-jose, passlib   → auth here is DRF TokenAuthentication + OTP,
#                             not JWT
#    PyPDF2, python-docx     → not imported anywhere (reportlab, which IS
#                             used by export_service.py, is kept above)
#    scikit-learn, scipy, joblib, numpy, nltk → no ML/NLP code found;
#                             predictions_service.py appears to be
#                             rule-based, not model-based
#    npm==0.1.1             → not a real PyPI package for the Node CLI;
#                             this was almost certainly a mistaken entry